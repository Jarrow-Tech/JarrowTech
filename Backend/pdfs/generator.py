# import pyPdf
# import io
# import tkinter
# from reportlab.pdfgen import canvas

# # called to generate a pdf for the wyoming hemp cultivator liscense
# # takes a json object with all of the data to fill in the pdf
def generateWyomingLiscense(data):
    # with open("hempliscenseapp.pdf") as hlaPdfFile:
    #     input = pyPdf.PdfFileReader(hlaPdfFile)
    

    # packet = io.StringIO()
    # can = tkinter.Canvas(packet)
    # can.create_oval(50, 50, 100, 100)
    # can.save()
    # packet.seek(0)
    # text = pyPdf.PdfFileReader(packet)

    # can = canvas.Canvas("hello.pdf")
    # can.drawString(100, 750, "Welcome to ReportLab!")
    # can.save()


    return True